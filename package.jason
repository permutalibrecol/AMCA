import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://tusuario.github.io/amca-web-astro',
  output: 'static',
  compressHTML: true,
  build: {
    inlineStylesheets: 'auto'
  }
});